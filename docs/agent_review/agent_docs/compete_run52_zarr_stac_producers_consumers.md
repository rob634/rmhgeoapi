# COMPETE Run — Gamma + Delta Report\n\n**Scope**: STAC Metadata Producers (Alpha) vs Consumers (Beta)\n**Date**: 23 MAR 2026\n**Files Under Review**: `handler_materialize_item.py`, `handler_materialize_collection.py`, `stac_materialization.py`, `zarr_metadata_repository.py`, `handler_register.py`, `handler_persist_tiled.py`, `pgstac_repository.py`, `blob.py`\n\n---\n\n# PART 1: GAMMA REPORT (Contradiction Finder)\n\n## 1. CONTRADICTIONS\n\n**CONTRADICTION-1: Alpha C2 vs actual code path -- severity disputed**\n\nAlpha claims `get_xarray_storage_options()` \"will crash when BlobRepository is initialized with connection_string\" because `self.credential` is never set. This is TRUE at the code level (`blob.py` line 294-296 vs line 303), but Alpha notes the zarr handler calls `for_zone(\"silver\")` which always uses `DefaultAzureCredential`. The `for_zone()` classmethod (`blob.py` line 400-403) constructs with `account_name` only, never `connection_string`. Alpha rates this HIGH; I recalibrate to **MEDIUM-LATENT** -- the bug exists but is unreachable by current zarr code paths. It becomes HIGH only if zarr ingestion ever reads from a connection-string-based account.\n\n**CONTRADICTION-2: Beta F7 vs Constitution -- heuristic vs fallback**\n\nBeta flags `_is_vector_release` fallback heuristic as a Principle 1 violation. On inspection (`stac_materialization.py` lines 786-793), the heuristic logs a warning before returning `True`. This is NOT fully silent -- it is logged. However, the consequence is that a raster/zarr release with NULL `blob_path` and NULL `stac_item_json` would be silently skipped (no STAC materialization). The logging is present (Principle 11 satisfied), but the accommodation is still silent from the caller's perspective (Principle 1 violated). **Recalibrate: LOW** -- the edge case requires two NULL fields on a non-vector release, which implies a producer bug upstream (caught by Alpha C4/C8 concerns). The heuristic is a reasonable defensive layer.\n\n## 2. AGREEMENT REINFORCEMENT\n\n**AGREE-1 (CONFIRMED): Silent exception swallowing -- Alpha C3 + Beta F2**\n\nAlpha found `except Exception: pass` in `handler_register.py` lines 118-126 (spatial extraction) and 134-139 (temporal extraction). Beta independently found `except Exception: pass` in `handler_materialize_item.py` lines 78-79 and 89-90 (metadata lookups). Same anti-pattern in both producer AND consumer. This is a systemic pattern violating Standard 3.3 and Principle 11. Both reviewers correctly identified it. **CONFIRMED, HIGH across both files.**\n\n**AGREE-2 (CONFIRMED): ZarrMetadataRepository f-string SQL**\n\nAlpha flagged this as C1 CRITICAL. Beta noted it in VS8 (\"outside my primary scope but flagged as context\"). Both reviewers identified the f-string SQL in `zarr_metadata_repository.py` lines 94-98. **CONFIRMED, CRITICAL per Standard 1.2 zero-tolerance rule.**\n\n## 3. BLIND SPOTS\n\n**BLIND-1: Producer-consumer contract gap on `blob_path` for zarr items**\n\nNeither reviewer fully connected the dots on this cross-boundary issue. The producer (`handler_register.py`) writes `store_url` (an `abfs://` URL) to `zarr_metadata`, NOT `blob_path`. The consumer (`handler_materialize_item.py` line 76-77) looks for `blob_path` in `cog_metadata` and uses it for TiTiler URL injection. For zarr items, there is no `blob_path` -- TiTiler URLs are irrelevant (zarr is not a COG). Beta caught the crash symptom (F1), but neither reviewer noted that the TiTiler injection at line 111-112 is semantically wrong for zarr items -- it will silently skip injection because `effective_blob_path` will be None/falsy after F1 is fixed. This is benign (TiTiler doesn't serve zarr), but it's an undocumented contract assumption.\n\n**BLIND-2: `stac_extensions` is empty array in zarr producer**\n\n`handler_register.py` line 185 writes `\"stac_extensions\": []`. The consumer's sanitizer (`stac_materialization.py` line 110-117) strips `processing:*` extension URLs. For zarr items, the empty array means sanitization is a no-op, which is fine. But the zarr producer includes `xarray:open_kwargs` in properties -- this is a custom extension that arguably should be listed in `stac_extensions` per STAC spec. Neither reviewer caught this spec non-compliance.\n\n**BLIND-3: `pgstac.upsert_item` SQL call is NOT parameterized via `sql.SQL`**\n\n`pgstac_repository.py` line 300: `cur.execute(\"SELECT * FROM pgstac.upsert_item(%s)\", (item_json,))`. The function name `pgstac.upsert_item` is a string literal, not composed via `sql.Identifier`. Beta marked this SAFE in VS8. Strictly, the schema `pgstac` is hardcoded in the SQL string. Since this is a well-known pgSTAC function with a fixed name, this is pragmatically safe, but technically a Standard 1.2 gray area. **ACCEPTED RISK** -- pgSTAC function names are stable API.\n\n## 4. SEVERITY RECALIBRATION\n\n| ID | Source | Original | Recalibrated | Confidence | Rationale |\n|----|--------|----------|--------------|------------|-----------|\n| Beta-F1 | Beta | CRITICAL | **CRITICAL** | CONFIRMED | Verified: `cog_metadata` undefined/None on zarr path, line 110 crashes |\n| Alpha-C1 | Alpha | CRITICAL | **CRITICAL** | CONFIRMED | f-string SQL, Standard 1.2 zero-tolerance. Both reviewers flagged. |\n| Alpha-C3 | Alpha | HIGH | **HIGH** | CONFIRMED | Silent swallow in producer. Same pattern as Beta-F2. |\n| Beta-F2 | Beta | HIGH | **HIGH** | CONFIRMED | Silent swallow in consumer. Same pattern as Alpha-C3. |\n| Alpha-C4 | Alpha | HIGH | **HIGH** | CONFIRMED | Global bbox fallback, Principle 1 violation. Verified at line 151. |\n| Beta-F3 | Beta | HIGH | **HIGH** | CONFIRMED | No contract validation before pgSTAC write. Verified. |\n| Alpha-C5 | Alpha | HIGH | **MEDIUM** | PROBABLE | Partial success reporting. Handler does include counts; orchestrator could check. |\n| Alpha-C2 | Alpha | HIGH | **MEDIUM-LATENT** | PROBABLE | `self.credential` missing on connection-string path. Currently unreachable. |\n| Beta-F4 | Beta | MEDIUM | **LOW** | CONFIRMED | Repo per-call. Config uses singletons internally; connection overhead is modest. |\n| Beta-F5 | Beta | MEDIUM | **MEDIUM** | CONFIRMED | `get_collection` returns None on DB error. Verified at line 344-346. |\n| Beta-F6 | Beta | MEDIUM | **MEDIUM** | CONFIRMED | Same pattern as F5 for `compute_collection_extent`. |\n| Beta-F7 | Beta | MEDIUM | **LOW** | SPECULATIVE | Heuristic is logged and requires two NULLs on non-vector. |\n| Alpha-C6 | Alpha | MEDIUM | **MEDIUM** | CONFIRMED | \"NOW()\" sentinel pattern is fragile. |\n| Alpha-C7 | Alpha | MEDIUM | **LOW** | CONFIRMED | Different JSON serialization strategies. Works correctly in both. |\n| Alpha-C8 | Alpha | MEDIUM | **MEDIUM** | CONFIRMED | Missing zarr chunk/compression metadata. 5 columns always NULL. |\n| Alpha-C9 | Alpha | MEDIUM | **LOW** | CONFIRMED | width=0, height=0 for tiles. Documented in comment. |\n| Alpha-C10 | Alpha | MEDIUM | **LOW** | CONFIRMED | Hardcoded sentinel date. Consistent with other handlers. |\n| Alpha-C11 | Alpha | LOW | **LOW** | CONFIRMED | \"unknown\" defaults. Minor Principle 1 deviation. |\n| Alpha-C12 | Alpha | LOW | **LOW** | CONFIRMED | Same as C11 for run_id. |\n\n---\n\n# PART 2: DELTA REPORT (Final Arbiter)\n\n## EXECUTIVE SUMMARY\n\nThe STAC producer-consumer boundary has two confirmed showstopper defects and one systemic anti-pattern. Beta-F1 is a NameError/AttributeError that will crash every zarr materialization through the new DAG handler -- this is the highest-priority fix. Alpha-C1 is an f-string SQL construction in `ZarrMetadataRepository.upsert()` that violates the project's zero-tolerance SQL injection rule. The systemic pattern of `except Exception: pass` (found independently by both reviewers in different files) silently swallows database errors, misclassifies transient failures as permanent, and makes debugging impossible. On the positive side, the architecture is sound: BlobRepository credential discipline is exemplary, handler return contracts are consistent, pgSTAC writes are idempotent, and the copy-before-mutate pattern prevents source data corruption.\n\n## TOP 5 FIXES\n\n### FIX 1: Beta-F1 -- NameError crash on zarr materialization path\n\n- **WHAT**: `cog_metadata` variable is undefined or None when the zarr lookup path is taken; line 110 references `cog_metadata.get(\"blob_path\")` unconditionally.\n- **WHY**: Every zarr item materialized through the DAG handler will crash. This blocks all zarr STAC materialization.\n- **WHERE**: `/Users/robertharrison/python_builds/rmhgeoapi/services/stac/handler_materialize_item.py`, line 110\n- **HOW**: Initialize `cog_metadata = None` before the try block (line 69). Replace line 110 with: `effective_blob_path = blob_path or (cog_metadata.get(\"blob_path\") if cog_metadata else None)`\n- **EFFORT**: 10 minutes\n- **RISK**: Zero -- purely defensive fix, no behavior change for raster path\n\n### FIX 2: Alpha-C1 -- SQL injection in ZarrMetadataRepository.upsert()\n\n- **WHAT**: f-string SQL construction with dynamic column names from kwargs.\n- **WHY**: Standard 1.2 zero-tolerance violation. Current callers are safe (known column names), but the pattern is a precedent violation and a future risk.\n- **WHERE**: `/Users/robertharrison/python_builds/rmhgeoapi/infrastructure/zarr_metadata_repository.py`, lines 94-98\n- **HOW**: Rewrite to use `sql.SQL` + `sql.Identifier` for schema/table/column names and `%s` placeholders for values. Validate kwargs keys against a whitelist of known `zarr_metadata` columns. Follow the `RasterMetadataRepository` pattern.\n- **EFFORT**: 1-2 hours\n- **RISK**: Low -- test with existing zarr ingest pipeline after change\n\n### FIX 3: Alpha-C3 + Beta-F2 -- Silent exception swallowing (systemic)\n\n- **WHAT**: `except Exception: pass` in both producer (spatial/temporal extraction) and consumer (metadata lookups).\n- **WHY**: Violates Standard 3.3 and Principle 11. Transient DB errors become permanent \"not found\" failures. No diagnostic signal for debugging.\n- **WHERE**: `/Users/robertharrison/python_builds/rmhgeoapi/services/zarr/handler_register.py` lines 125-126, 138-139; `/Users/robertharrison/python_builds/rmhgeoapi/services/stac/handler_materialize_item.py` lines 78-79, 89-90\n- **HOW**: Replace `pass` with `logger.warning(\"...: %s\", exc)` in all four locations. In the consumer, additionally distinguish DB connection errors (retryable) from row-not-found (not retryable).\n- **EFFORT**: 30 minutes\n- **RISK**: Zero -- adds logging, does not change control flow\n\n### FIX 4: Alpha-C4 -- Global bbox fallback masks missing spatial data\n\n- **WHAT**: `bbox = spatial_extent or [-180, -90, 180, 90]` writes a global bounding box when spatial extraction fails.\n- **WHY**: Violates Principle 1. Downstream spatial queries return false positives. A dataset without extractable coordinates should not claim to cover the globe.\n- **WHERE**: `/Users/robertharrison/python_builds/rmhgeoapi/services/zarr/handler_register.py`, line 151\n- **HOW**: If `spatial_extent` is None, return `success: False` with an explicit error indicating coordinates could not be extracted. Or write NULL bbox to zarr_metadata and let the consumer handle it.\n- **EFFORT**: 30 minutes\n- **RISK**: Low -- may cause existing datasets with non-standard coordinate names (see Alpha A3) to fail, which is the desired behavior per Principle 1\n\n### FIX 5: Beta-F3 -- No STAC item contract validation before pgSTAC write\n\n- **WHAT**: The consumer writes `stac_item_json` to pgSTAC without validating required STAC fields (`geometry`, `bbox`, `properties`).\n- **WHY**: Violates Principle 10. Malformed JSON from a producer bug will produce cryptic pgSTAC errors instead of a clear contract violation.\n- **WHERE**: `/Users/robertharrison/python_builds/rmhgeoapi/services/stac/handler_materialize_item.py`, between lines 103 and 106\n- **HOW**: Add a minimal field presence check: `required = {\"geometry\", \"bbox\", \"properties\", \"assets\"}; missing = required - set(stac_item_json.keys()); if missing: return failure`\n- **EFFORT**: 15 minutes\n- **RISK**: Zero -- purely additive validation\n\n## ACCEPTED RISKS\n\n| ID | Description | Rationale |\n|----|-------------|-----------|\n| Alpha-C2 | `get_xarray_storage_options()` crashes on connection-string BlobRepository | Currently unreachable -- `for_zone(\"silver\")` never uses connection strings. Fix when silverext zarr support is added. |\n| Alpha-C5 | Partial tile persist reports success=True | Handler includes `tiles_persisted` vs `total_tiles` counts. Orchestrator can check. Changing to failure would break retry semantics for already-persisted tiles. |\n| Beta-F4 | PgStacRepository instantiated per call | Config layer uses singletons internally. Connection overhead is per-operation regardless. Not worth a singleton refactor now. |\n| Beta-F7 | `_is_vector_release` heuristic fallback | Logged, requires two NULLs on non-vector. Extremely unlikely edge case. Fix if asset lookup reliability improves. |\n| Alpha-C9 | width=0, height=0 for tiles | Documented in code comment. No downstream query currently filters on tile dimensions. |\n| BLIND-3 | pgSTAC function name hardcoded in SQL string | pgSTAC function names are a stable API contract. Standard 1.2 is intended for dynamic identifiers, not well-known function calls. |\n\n## ARCHITECTURE WINS\n\n1. **BlobRepository credential discipline** -- Both reviewers confirmed zero credential leakage. `for_zone()` singleton pattern is textbook Standard 1.5 compliance. No manual credential construction anywhere in the reviewed handlers.\n\n2. **Idempotent pgSTAC writes** -- `upsert_item` and `upsert_collection` are idempotent. Concurrent materialization of the same collection is safe (Beta VS1, E3). This enables fearless retry and rebuild.\n\n3. **Copy-before-mutate pattern** -- Both `handler_materialize_item.py` line 101 and `stac_materialization.py` line 210 copy the dict before mutation. Source metadata is never corrupted by consumer-side transformations (Beta VS7).\n\n4. **Consistent handler return contract** -- All six reviewed handlers (`zarr_register_metadata`, `zarr_batch_blobs`, `raster_persist_tiled`, `stac_materialize_item`, `stac_materialize_collection`, and the STACMaterializer methods) return `{\"success\": True/False, ...}` with `error_type` and `retryable` on failure. Standard 3.2 fully satisfied.\n\n5. **pgSTAC as materialized view** -- The architecture correctly treats pgSTAC as a derived store. Internal metadata tables (`cog_metadata`, `zarr_metadata`) are the source of truth. The `stac_materialize_item` handler is the single write path. This enables safe catalog rebuilds from internal state (Principle 2)."}],"stop_reason":"end_turn","stop_sequence":null,"usage":{"input_tokens":1,"cache_creation_input_tokens":1360,"cache_read_input_tokens":46561,"output_tokens":4237,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":0,"ephemeral_5m_input_tokens":1360},"inference_geo":"","iterations":[],"speed":"standard"}},"requestId":"req_011CZLiGd1GCzJXJoA3QbJDW","type":"assistant","uuid":"45aaae67-17ab-4405-8274-fdc2dc20cae0","timestamp":"2026-03-23T21:22:41.290Z","userType":"external","entrypoint":"cli","cwd":"/Users/robertharrison/python_builds/rmhgeoapi","sessionId":"2eabe264-552b-4d6f-82cd-aed30b008ec7","version":"2.1.79","gitBranch":"master","slug":"staged-crafting-lark"}
