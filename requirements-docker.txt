# ============================================================================
# Docker Worker Dependencies
# ============================================================================
# Same as requirements.txt MINUS azure-functions, PLUS FastAPI/uvicorn
# and VirtualiZarr pipeline packages.
#
# NOTE: GDAL is NOT listed here - it comes pre-installed in the base image
# (ghcr.io/osgeo/gdal:ubuntu-full-3.10.1). The Dockerfile rebuilds GDAL
# Python bindings after pip install to match the numpy 2.x ABI.
#
# Updated: 01 MAR 2026 - numpy 2.x migration, CVE audit, VirtualiZarr deps
# ============================================================================

# === Azure SDK ===
azure-storage-blob>=12.20.0
azure-storage-queue>=12.9.0
azure-data-tables>=12.5.0
azure-identity>=1.16.1
azure-keyvault-secrets>=4.8.0
azure-servicebus>=7.11.0
azure-mgmt-datafactory>=4.0.0
azure-monitor-opentelemetry>=1.6.0

# === Geospatial Core ===
geopandas>=1.1.2                      # CVE-2025-69662 (SQL injection) fixed in 1.1.2
psycopg[binary]>=3.2.0
rasterio>=1.4.2
rio-cogeo>=5.0.0
rio-stac>=0.10.0
cogeo-mosaic>=7.0.0

# === Schema Validation ===
pydantic>=2.0.0

# === NumPy 2.x ===
# GDAL 3.10.1 supports numpy 2.x. Dockerfile rebuilds GDAL Python bindings
# after pip install to recompile against numpy 2.x ABI. Required by virtualizarr.
numpy>=2.0.0,<3

# === Profiling ===
psutil>=5.9.0

# === STAC Implementation ===
pypgstac==0.9.8                       # Must match pgstac database schema version
psycopg-pool>=3.1.0
pystac>=1.13.0
pystac-client>=0.7.0
stac-pydantic>=3.4.0

# === DuckDB ===
duckdb>=1.1.0
pyarrow>=18.0.0

# === H3 ===
h3>=4.0.0

# === Raster / STAC Utilities ===
rasterstats>=0.19.0
planetary-computer>=1.0.0

# === Async PostgreSQL ===
asyncpg>=0.29.0

# === xarray ecosystem (Zarr direct access) ===
xarray>=2024.10.0
zarr>=2.18.0,<3                       # Stay on zarr 2.x for stability
fsspec>=2024.2.0
adlfs>=2024.2.0
aiohttp>=3.13.3                       # CVE-2025-69223 + 5 others fixed in 3.13.3

# === VirtualiZarr pipeline (NetCDF → virtual Zarr references) ===
h5py>=3.12.0                          # numpy 2.x compatible
virtualizarr>=1.1.0,<2.0.0           # zarr 2.x compatible range
kerchunk==0.2.8                       # Last version supporting zarr 2.x

# === HTTP / Image ===
httpx>=0.27.0
pillow>=12.1.1                        # CVE-2026-25990 (OOB write) fixed in 12.1.1

# === Docker Worker HTTP Server ===
fastapi>=0.115.0
uvicorn>=0.34.0
jinja2>=3.1.6                         # CVE-2025-27516 (sandbox breakout) fixed in 3.1.6

# === Security (transitive deps with known CVEs - pinned explicitly) ===
cryptography>=46.0.5                  # CVE-2026-26007 (subgroup attack) fixed in 46.0.5
urllib3>=2.6.3                        # CVE-2026-21441 (decompression DoS) fixed in 2.6.3
certifi>=2024.7.4                     # Ensure current CA bundle
requests>=2.32.3                      # CVE-2024-35195 (verify bypass) fixed in 2.32.2
setuptools>=78.1.1                    # CVE-2025-47273 (path traversal) fixed in 78.1.1
