# DO NOT include azure-functions-worker in this file
# The Python Worker is managed by Azure Functions platform
# Manually managing azure-functions-worker may cause unexpected issues

azure-functions
azure-storage-blob
azure-storage-queue
azure-data-tables
azure-identity
azure-keyvault-secrets
azure-servicebus>=7.11.0
azure-mgmt-datafactory>=4.0.0
geopandas
psycopg[binary]
rasterio>=1.3.1
rio-cogeo
rio-stac>=0.10.0
cogeo-mosaic>=7.0.0

# STRONG TYPING DISCIPLINE - Schema validation and enforcement
pydantic>=2.0.0

# DEBUG MODE - Memory and performance profiling
psutil>=5.9.0

# STAC Implementation - PgSTAC database and Python libraries
# Upgraded to 0.9.8 (13 NOV 2025) - Matches database schema version
pypgstac==0.9.8
psycopg-pool>=3.1.0
# Do not use psycopg-pool in code- dependency for pgstac only
pystac>=1.13.0
pystac-client>=0.7.0
stac-pydantic>=3.4.0
planetary-computer>=1.0.0     # Planetary Computer STAC URL signing

# Raster Analysis - Zonal statistics on COGs
rasterstats>=0.19.0           # Zonal statistics for H3 aggregation

# DuckDB - Analytical query engine with spatial and Azure extensions
duckdb>=1.1.0
pyarrow>=10.0.0

# H3 - Uber's hexagonal hierarchical geospatial indexing system (Python bindings to C library)
h3>=4.0.0

# AsyncPG - Async PostgreSQL driver for non-blocking database operations
asyncpg>=0.29.0

# xarray ecosystem - Service Layer API for Zarr direct access (18 DEC 2025)
xarray>=2024.1.0
zarr>=2.16.0
fsspec>=2024.2.0
adlfs>=2024.2.0           # Azure Blob storage backend for fsspec
aiohttp>=3.9.0            # Async support for fsspec

# HTTP client for TiTiler proxy (18 DEC 2025)
httpx>=0.27.0

# Image output for xarray API (18 DEC 2025)
pillow>=10.0.0