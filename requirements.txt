# DO NOT include azure-functions-worker in this file
# The Python Worker is managed by Azure Functions platform
# Manually managing azure-functions-worker may cause unexpected issues

azure-functions
azure-storage-blob
azure-storage-queue
azure-data-tables
azure-identity

# Geospatial processing libraries
rasterio>=1.3.0
rio-cogeo>=5.0.0
Pillow>=10.0.0  # For TIFF tag and EXIF extraction

# PostGIS and spatial libraries
psycopg[binary]>=3.1.0  # psycopg3 with binary support
shapely>=2.0.0